server:
  port: 8085

spring:
  application:
    name: myvalue
  profiles:
    active: test
  cloud:
    config: 
      enabled: true
      uri: http://localhost:8888
      
management:
  endpoints:
    web:
      exposure:
        include: '*'